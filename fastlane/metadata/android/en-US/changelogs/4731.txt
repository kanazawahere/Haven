- MCP: terminal tools (read_terminal_snapshot / read_terminal_scrollback / send_terminal_input / queue_terminal_input) now return a clear "no terminal session is connected — connect a profile first" error instead of an opaque failure when nothing is connected. When exactly one terminal is open, sessionId can be omitted and defaults to it. Stops external agents looping with no feedback. (#213)
