📶 **Mosh: the "no server contact — retrying" banner no longer gets stuck** — when a mosh session actually ended (server exited or a fatal transport error), the red "No server contact for Ns — retrying" banner could freeze on screen forever, reading as a reconnect that never happens. It now clears when the transport closes, so it only shows while a live connection is genuinely stalling. First fix toward #421; the underlying scroll-triggered disconnect is still being investigated. (#421, thanks dkoppenh)
