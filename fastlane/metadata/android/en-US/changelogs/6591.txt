• Files: the show-hidden (eye) toggle is global, but switching between Files tabs used to leave each tab filtered as it was before — so turning show-hidden on in one tab and switching to another showed the "showing hidden" icon while that tab still hid dotfiles. Each tab now re-applies the current show-hidden and name-filter when you switch to it, so the list always matches the icon.
