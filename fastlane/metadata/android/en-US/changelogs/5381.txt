Reliability fix for the agent (MCP) file-browser verbs added in 5.59.83/84.

encrypt_file, decrypt_file and navigate to the file browser now work even when issued as the very first command after launch, before the Files tab has opened — previously such a command could be dropped (or, in a debug build, crash on a race). No change to normal app use.
